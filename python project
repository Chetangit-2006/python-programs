print("Welcome to English Dictionary Application")

//dictionary words
{
"apple": ["A fruit that grows on trees."],
"banana": ["A long curved fruit with yellow skin."],
"computer": ["An electronic device used to process data."],
"python": ["A programming language.", "A large snake."],
"keyboard": ["Input device used for typing."],
"monitor": ["Output device used to display images."],
"internet": ["A global network connecting computers."],
"algorithm": ["A step-by-step process to solve a problem."],
"database": ["Organized collection of data."],
"software": ["Programs used to operate computers."],
"hardware": ["Physical parts of a computer."],
"network": ["A group of connected computers."],
"developer": ["Person who writes computer programs."],
"data": ["Information processed by computers."],
"program": ["Set of instructions for a computer."]
"laptop": ["A portable personal computer."],
"mouse": ["A handheld pointing device used to control the cursor."],
"printer": ["A device that prints text or images onto paper."],
"scanner": ["A device that converts documents into digital format."],
"server": ["A computer that provides data or services to other computers."],
"client": ["A computer that requests services from a server."],
"application": ["A software program designed to perform a specific task."],
"browser": ["A software application used to access websites."],
"website": ["A collection of related web pages available on the internet."],
"email": ["A system for sending messages electronically."],
"password": ["A secret word used to access a system."],
"user": ["A person who uses a computer system."],
"file": ["A collection of data stored on a computer."],
"folder": ["A container used to organize files."],
"backup": ["A copy of data stored to prevent loss."],
"virus": ["A malicious program that harms a computer system."],
"firewall": ["A security system that controls network traffic."],
"encryption": ["The process of converting data into a secure form."],
"update": ["A new version of software with improvements."],
"download": ["The process of receiving data from the internet."],
"upload": ["The process of sending data to the internet."],
"cloud": ["Internet-based storage and computing services."],
"coding": ["The process of writing computer programs."],
"debugging": ["The process of finding and fixing errors in code."],
"variable": ["A storage location used to store data in programming."],
"function": ["A reusable block of code that performs a specific task."],
"loop": ["A programming structure that repeats a set of instructions."],
"condition": ["A rule used to control program flow."],
"syntax": ["The set of rules that define program structure."],
"compiler": ["A program that converts code into machine language."]
}
/// code
import json
from difflib import get_close_matches

# Load dictionary data
data = json.load(open("data.json"))

# Function to search word
def translate(word):

    word = word.lower()

    if word in data:
        return data[word]

    elif len(get_close_matches(word, data.keys())) > 0:

        suggestion = get_close_matches(word, data.keys())[0]

        choice = input("Did you mean '%s'? Enter Y if yes or N if no: " % suggestion)

        if choice.lower() == "y":
            return data[suggestion]

        elif choice.lower() == "n":
            return ["Word does not exist. Please check spelling."]

        else:
            return ["Invalid input."]

    else:
        return ["Word not found in dictionary."]


print("-------- English Dictionary Application --------")

while True:

    word = input("\nEnter a word (type 'exit' to quit): ")

    if word.lower() == "exit":
        print("Thank you for using the dictionary!")
        break

    output = translate(word)

    for meaning in output:
        print("-", meaning)
